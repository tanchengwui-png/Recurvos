import { useEffect, useMemo, useState } from "react";
import { useNavigate, useParams } from "react-router-dom";
import { ConfirmModal } from "../components/ConfirmModal";
import { HelperText } from "../components/ui/HelperText";
import { FormPageHeader } from "../components/ui/FormPageHeader";
import { SearchableSelect } from "../components/ui/SearchableSelect";
import { api } from "../lib/api";
import { formatCurrency } from "../lib/format";
import type { Account, JournalEntry, MasterDataSnapshot } from "../types";

type Line = { accountId: string; description: string; debitAmount: number; creditAmount: number };
const emptyLine = (): Line => ({ accountId: "", description: "", debitAmount: 0, creditAmount: 0 });

export function JournalEntryFormPage() {
  const { id } = useParams(); const navigate = useNavigate();
  const [accounts, setAccounts] = useState<Account[]>([]); const [journalDateUtc, setJournalDateUtc] = useState(new Date().toISOString().slice(0, 10)); const [referenceNo, setReferenceNo] = useState(""); const [description, setDescription] = useState(""); const [lines, setLines] = useState<Line[]>([emptyLine(), emptyLine()]); const [error, setError] = useState(""); const [confirm, setConfirm] = useState(false);
  useEffect(() => { void (async () => { const [snapshot, journal] = await Promise.all([api.get<MasterDataSnapshot>("/master-data"), id ? api.get<JournalEntry>(`/accounting/journal-entries/${id}`) : Promise.resolve(null)]); setAccounts(snapshot.accounts); if (journal) { setJournalDateUtc(journal.journalDateUtc.slice(0, 10)); setReferenceNo(journal.referenceNo); setDescription(journal.description); setLines(journal.lines.map(x => ({ accountId: x.accountId, description: x.description, debitAmount: x.debitAmount, creditAmount: x.creditAmount }))); } })(); }, [id]);
  const availableAccounts = useMemo(() => accounts.filter(x => x.isActive && x.allowManualEntries), [accounts]);
  const accountOptions = useMemo(() => availableAccounts.map(x => ({ value: x.id, label: `${x.code} · ${x.name}`, keywords: [x.code, x.name] })), [availableAccounts]);
  const debit = lines.reduce((sum, x) => sum + (Number(x.debitAmount) || 0), 0); const credit = lines.reduce((sum, x) => sum + (Number(x.creditAmount) || 0), 0);
  const balanced = lines.length >= 2 && debit > 0 && Math.round(debit * 100) === Math.round(credit * 100) && lines.every(x => !!x.accountId && ((x.debitAmount > 0) !== (x.creditAmount > 0)));
  const update = (index: number, value: Partial<Line>) => setLines(current => current.map((x, i) => i === index ? { ...x, ...value } : x));
  async function save() { try { setError(""); const payload = { journalDateUtc: new Date(`${journalDateUtc}T00:00:00Z`).toISOString(), referenceNo, description, lines: lines.map(x => ({ ...x, debitAmount: Number(x.debitAmount), creditAmount: Number(x.creditAmount) })) }; if (id) await api.put(`/accounting/journal-entries/${id}`, payload); else await api.post("/accounting/journal-entries", payload); navigate("/finance/journal-entries"); } catch (e) { setError(e instanceof Error ? e.message : "Unable to save journal entry."); } }
  return <div className="page"><FormPageHeader backLabel="Back to Journal Entries" backHref="/finance/journal-entries" breadcrumbs={<><span>Journal Entries</span><span>/</span><span>{id ? "Edit Journal Entry" : "Create Journal Entry"}</span></>} />{error ? <HelperText tone="error">{error}</HelperText> : null}<div className="master-data-form-grid master-data-form-grid-wide"><label className="form-label">Journal Date<input className="text-input" type="date" value={journalDateUtc} onChange={e => setJournalDateUtc(e.target.value)} /></label><label className="form-label">Reference<input className="text-input" value={referenceNo} onChange={e => setReferenceNo(e.target.value)} /></label><label className="form-label master-data-form-wide">Description<input className="text-input" value={description} onChange={e => setDescription(e.target.value)} /></label></div><section className="card"><div className="card-section-header"><h3 className="section-title">Journal Lines</h3><button type="button" className="button button-secondary" onClick={() => setLines(x => [...x, emptyLine()])}>+ Add Line</button></div><div className="table-scroll table-scroll-bounded"><table className="catalog-table"><thead><tr><th>Account</th><th>Description</th><th className="numeric-cell">Debit</th><th className="numeric-cell">Credit</th><th /></tr></thead><tbody>{lines.map((line, index) => <tr key={index}><td><SearchableSelect value={line.accountId} onChange={accountId => update(index, { accountId })} options={accountOptions} placeholder="Search account..." searchPlaceholder="Search account code or name..." emptyText="No eligible accounts found." ariaLabel="Account" portalPopover /></td><td><input className="text-input" value={line.description} onChange={e => update(index, { description: e.target.value })} /></td><td><input className="text-input numeric-cell" type="number" min="0" step="0.01" value={line.debitAmount} onChange={e => update(index, { debitAmount: Number(e.target.value), creditAmount: Number(e.target.value) > 0 ? 0 : line.creditAmount })} /></td><td><input className="text-input numeric-cell" type="number" min="0" step="0.01" value={line.creditAmount} onChange={e => update(index, { creditAmount: Number(e.target.value), debitAmount: Number(e.target.value) > 0 ? 0 : line.debitAmount })} /></td><td><button type="button" className="button button-secondary button-compact" disabled={lines.length <= 2} onClick={() => setLines(x => x.filter((_, i) => i !== index))}>Remove</button></td></tr>)}</tbody></table></div><div className="page-meta-row page-meta-row-inline"><div className="page-meta-chips"><span className="page-meta-chip">Debit <strong>{formatCurrency(debit, "MYR")}</strong></span><span className="page-meta-chip">Credit <strong>{formatCurrency(credit, "MYR")}</strong></span><span className="page-meta-chip">{balanced ? "Balanced" : "Not balanced"}</span></div></div><div className="contact-page-actions"><button type="button" className="button button-secondary" onClick={() => navigate("/finance/journal-entries")}>Cancel</button><button type="button" className="button button-primary" disabled={!balanced || !description.trim()} onClick={() => setConfirm(true)}>{id ? "Save Changes" : "Create Journal"}</button></div></section><ConfirmModal open={confirm} title={id ? "Save journal changes" : "Create journal entry"} description="Save this balanced draft journal entry?" confirmLabel={id ? "Save Changes" : "Create Journal"} onConfirm={save} onCancel={() => setConfirm(false)} /></div>;
}
